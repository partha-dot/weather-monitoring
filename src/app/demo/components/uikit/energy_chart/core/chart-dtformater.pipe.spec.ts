import { ChartDTFormaterPipe } from './chart-dtformater.pipe';

describe('ChartDTFormaterPipe', () => {
  it('create an instance', () => {
    const pipe = new ChartDTFormaterPipe();
    expect(pipe).toBeTruthy();
  });
});
